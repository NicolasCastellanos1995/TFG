<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TFGApp"
        android:usesCleartextTraffic="true">

        <activity android:name=".ui.home.SupervisorActivity" />
        <activity android:name=".ui.home.OperarioActivity" />
        <activity android:name=".ui.common.EntradasActivity" />
        <activity android:name=".ui.common.SalidasActivity" />
        <activity android:name=".ui.common.TransferenciasActivity" />
        <activity android:name=".ui.common.ConsultasStockActivity" />
        <activity android:name=".ui.common.HistorialMovimientosActivity" />
        <activity android:name=".ui.supervisor.CrearCategoriaActivity" />
        <activity android:name=".ui.supervisor.CrearProductoActivity" />
        <activity android:name=".ui.supervisor.CrearUbicacionActivity" />
        <activity
            android:name=".ui.supervisor.EditarActivity"
            android:exported="false" />

        <activity
            android:name=".ui.supervisor.CrearActivity"
            android:exported="false" />

        <activity
            android:name=".ui.supervisor.AlertasActivity"
            android:exported="false" />

        <activity
            android:name=".ui.login.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>